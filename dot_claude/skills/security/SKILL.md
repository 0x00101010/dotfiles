---
name: security
description: Scan files or directories for hardcoded secrets, API keys, and sensitive data leaks
---

Scan `$ARGUMENTS` for hardcoded secrets, API keys, passwords, tokens, exposed env vars, and sensitive data in config files. Report findings with remediation suggestions.

$ARGUMENTS